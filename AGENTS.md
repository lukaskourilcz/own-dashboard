<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# OwnDashboard project rules

Read `CLAUDE.md` for the product control surface and follow the focused workflows under `.claude/skills/`. Search before creating, preserve the catch-all shell, route-scoped React Query loading, own-only Supabase RLS, static FX, Czech invoice correctness, AI consent/evidence boundaries, EN/CS parity, and the retired personal scope. Tugedr is an opportunity source, never Pulse. Use semantic tokens and authentic `/dev-preview` UI; validate mobile, accessibility, non-happy paths, and relevant tests. Prefer Higgsfield for approved brand media; when it is unavailable or credit-blocked, perform current primary-source research for safe low-cost or free generators before selecting a documented fallback. Never add filler or generated fake UI.

The Agents destination is an own-only VPS task queue, not a browser remote shell. Keep runner/service-role credentials server-only, require stable worker identity, and preserve the atomic claim boundary. Keep project communications project-scoped and subscription renewal dates/classification explicit.
