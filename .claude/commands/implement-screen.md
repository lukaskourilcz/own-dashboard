# /implement-screen

1. Read `AGENTS.md`, `CLAUDE.md`, product/design skills, and the target workflow.
2. Trace route-scoped data, queries, mutations, relationships, permissions, and existing tests before editing.
3. Search for reusable tokens/components; avoid duplicate storage or UI libraries.
4. Implement hierarchy, states, actions, localized statuses, EN/CS copy, responsive transformations, keyboard/focus behavior, and explicit destructive/AI boundaries.
5. Preserve RLS, static FX, invoice correctness, integration reality, AI consent, and query invalidation.
6. Add/update focused unit and Playwright coverage.
7. Run targeted syntax/lint/type/tests/visual/axe checks, fix feasible issues, update authoritative docs if the pattern changed, and report exact evidence.
